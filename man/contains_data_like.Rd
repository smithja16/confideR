% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_leak_detection.R
\name{contains_data_like}
\alias{contains_data_like}
\title{Check whether an object contains data-like content}
\usage{
contains_data_like(x, max_vector_length = 500)
}
\arguments{
\item{x}{Object to inspect.}

\item{max_vector_length}{Atomic vectors longer than this are
flagged. Default 500.}
}
\value{
Logical scalar.
}
\description{
Returns \code{TRUE} if the object (or any nested element) looks
like it contains raw data that should not be shared externally.
This includes data frames, matrices, fitted model objects (which
embed raw data internally), and large atomic vectors.
}
\details{
Objects of class \code{confider_fingerprint} are always safe.
}
\examples{
contains_data_like(mtcars)          # TRUE
contains_data_like(lm(mpg ~ wt, mtcars))  # TRUE (model embeds data)
contains_data_like("hello")         # FALSE
contains_data_like(1:100)           # FALSE (under threshold)
}
